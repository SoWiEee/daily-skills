# Layout Routing 範例

## 範例 1：比較

輸入：比較 Windows 與 macOS 在 execution、privilege、lateral movement、identity target 上的差異。

路由：

`相同比較維度 → Matched Comparison → G12 → 50/50 Mirrored Columns → dense`

## 範例 2：Mechanism

輸入：解釋 Scheduler 怎麼選 GPU 與 MPS Percentage。

路由：

`ordered decision + resource allocation → Decision Flow + Allocation Diagram → G17 / G19 → Full-canvas → dense`

## 範例 3：Code

輸入：解釋 15 行建立 PyTorch DataLoader 的 Python Code。

路由：

`code semantics → Annotated Code → G25 → 55/45 Code + Explanation → dense`
